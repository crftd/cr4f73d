@import 'normalize.css';

html,
body {
  background-color: #000;
  height: 100%;
}

.wrapper {
  display: flex;
  flex-direction: column;
  height: 100%;
}

.landing {
  flex: 1 0 auto;
}

.footer {
  flex: 0 0 auto;
}
